services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: brief_user
      POSTGRES_PASSWORD: brief_pass
      POSTGRES_DB: brief_collector
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
